spring:
  application:
    name: account-service
  config:
    import: optional:configserver:http://18.60.109.81:8888

server:
  port: 8083
  
eureka:
  instance:
    prefer-ip-address: true
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://18.60.109.81:8761/eureka

debug: true 
